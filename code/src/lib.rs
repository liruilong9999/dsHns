//! DeepSeek 专属 Agent 后端库入口。
//!
//! 当前阶段先落地基础项目骨架与配置模块，为后续目录、会话、工具和智能体能力提供统一入口。

/// 应用层模块，后续承载 `CLI`、会话控制器等编排能力。
pub mod app;

/// 领域层模块，后续承载会话、工具、上下文与智能体等核心领域能力。
pub mod domain;

/// 基础设施层模块，后续承载配置、数据库、日志与外部环境访问能力。
pub mod infra;

/// 预留接口层模块，后续承载 `Tauri IPC` 契约与空实现。
pub mod ipc;
