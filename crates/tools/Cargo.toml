[package]
name = "dshns-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
dshns-core = { path = "../core" }
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
serde_json = "1"
regex = "1"
futures = "0.3"
